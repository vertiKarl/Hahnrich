function toggleNavbar() {
    const enabled = $('#navbar')[0].classList.toggle("enabled");
    //$('.bottom-popout')[0].style.width = enabled ? window.innerWidth - $('#navbar')[0].offsetWidth + "px" : "100vw";
}